import styled from 'styled-components';
import { StyledInputContainer } from '../AuthForm/styles';

const StyledForm = styled.form`
  margin: 0 auto;
  width: 60vw;
  max-width: 600px;
`;

export { StyledInputContainer, StyledForm };
