import styled from 'styled-components';
import StyledButton from '../../styles/components/StyledButton';

export const StyledCreateTaskButton = styled(StyledButton)`
  height: 40px;
  padding: 0 0.75em;
  margin-top: 4px;
`;
